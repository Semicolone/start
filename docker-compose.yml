version: '3.8'

services:
  api:
    build: ./api
    container_name: flow-api
    ports:
      - "8000:8000"
    volumes:
      - ./api/app:/app/app
    environment:
      - DEBUG=True
    restart: unless-stopped