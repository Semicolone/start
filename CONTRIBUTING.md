# 기여 가이드

## 브랜치 전략

### 주요 브랜치
- `main`: 프로덕션 브랜치 (안정화된 버전)
- `dev`: 개발 브랜치 (통합 및 테스트)

### 작업 브랜치 및 브랜치 네이밍
- `feat/#이슈번호-작업명`: 새 기능
- `fix/#이슈번호-작업명`: 버그 수정
- `docs/#이슈번호-작업명`: 문서
- `refactor/#이슈번호-작업명`: 리팩토링

### 머지 흐름
- 작업 브랜치 → dev (PR & 리뷰)
- dev → main (안정화 후)

예시: `feat/#3-user-login`, `fix/#7-calendar-crash`

---

## 커밋 메시지

| 타입 | 사용 상황 |
|------|-----------|
| `feat` | 새 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 수정 |
| `style` | 코드 포맷 변경 (로직 변경 없음) |
| `refactor` | 리팩토링 |
| `chore` | 빌드/설정 변경 |
| `test` | 테스트 추가/수정 |

예시:

---

## 작업 흐름

1. 이슈 생성
2. dev 브랜치 최신화 후 작업 브랜치 생성
3. 작업 후 PR 생성 — PR 본문에 `Closes #이슈번호` 포함
4. 코드 리뷰 후 **dev** 브랜치에 머지
5. 안정화 후 **main** 브랜치에 머지

### 주의사항
- ❌ main 브랜치에 직접 push 금지
- ✅ 항상 dev 브랜치를 거쳐서 main으로
- ✅ PR 전에 dev 브랜치 최신화 필수

---

## 디렉토리 구조

## CI/CD

- GitHub Actions를 통한 자동 테스트/배포
- dev 브랜치 push 시 자동 실행
- 테스트 통과 후 자동 배포

---

## 환경 설정

로컬 개발 시:
1. `.env.example` 복사 → `.env`
2. 실제 값으로 변경
3. Docker 실행: `docker-compose up -d`